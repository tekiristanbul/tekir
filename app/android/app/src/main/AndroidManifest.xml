<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- release builds need INTERNET explicitly (the flutter template only
         grants it in debug/profile manifests) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- geolocator: nearby-cats map/discover distances. denying it never
         blocks anything — both surfaces fall back to the istanbul center
         and give up only the distance column (docs/design/app-states.md,
         state 06) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- firebase_messaging on android 13+: runtime permission requested via
         the in-app opt-in sheet (features/notifications), never at launch -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- firebase_analytics merges advertising-id and attribution permissions
         into the manifest by default. the product never uses an advertising
         identifier, ad personalization, or attribution (docs/product/privacy.md,
         website/privacy.html), and leaving them in would force a contradicting
         "device or other ids" declaration in the play data-safety form — so
         they are removed at merge time and the analytics sdk is told not to
         collect the id (meta-data below). -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove"/>
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove"/>
    <application
        android:label="tekir"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- google_maps_flutter: the android maps sdk reads its key here; the
             value comes from the gitignored android/maps.properties via a
             manifest placeholder (see app/build.gradle.kts) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleMapsApiKey}" />
        <!-- matches the removed AD_ID permission above: no advertising id, no
             ad personalization signals, no attribution -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
